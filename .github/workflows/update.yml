name: 毎日BMSGダッシュボードを自動更新

on:
  schedule:
    # 毎朝9:00 JST（= UTC 00:00）に実行
    - cron: '0 0 * * *'
  workflow_dispatch:
    # 手動実行ボタンも使えるようにする

permissions:
  contents: write

jobs:
  update-dashboard:
    runs-on: ubuntu-latest
    
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Python 3.11 をセットアップ
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 依存パッケージをインストール
        run: pip install anthropic

      - name: ダッシュボードを生成
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python scripts/generate.py

      - name: index.html をコミット＆プッシュ
        run: |
          git config user.name "BMSG Dashboard Bot"
          git config user.email "bot@bmsg-dashboard"
          git add index.html
          git diff --staged --quiet || git commit -m "🔄 ダッシュボード自動更新 $(date +'%Y-%m-%d')"
          git push
